{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd book_management && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd book_management && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn book_management.wsgi",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
